<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Gemini Nano's model files are downloaded on first use via AICore. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- ML Kit GenAI Speech Recognition requires this even for file-based
         (AudioSource.fromPfd, ONE_SHOT) input, not just live mic streaming
         — confirmed by ERROR_TYPE_INSUFFICIENT_PERMISSION on a real device
         without it, 2026-08-26. Still needs a runtime request on API 23+. -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.DejaEntendu">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
