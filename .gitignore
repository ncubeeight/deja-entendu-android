*.iml
.gradle/
/local.properties
.idea/
.DS_Store
build/
/captures
.externalNativeBuild
.cxx
local.properties

# Release signing secrets — never commit. The actual .jks lives outside
# this repo entirely, in a sibling directory.
keystore.properties
*.jks
*.keystore
